diff --git a/node_modules/react-native-sound/android/src/main/java/com/zmxv/RNSound/Sound.kt b/node_modules/react-native-sound/android/src/main/java/com/zmxv/RNSound/Sound.kt
index 749ea14..e01ce63 100644
--- a/node_modules/react-native-sound/android/src/main/java/com/zmxv/RNSound/Sound.kt
+++ b/node_modules/react-native-sound/android/src/main/java/com/zmxv/RNSound/Sound.kt
@@ -19,8 +19,9 @@ import java.io.IOException
 
 
 open class Sound internal constructor(context:ReactApplicationContext):AudioManager.OnAudioFocusChangeListener  {
-  private var playerPool: MutableMap<Double, MediaPlayer> = mutableMapOf()
   private val reactContext: ReactApplicationContext = context
+  private val playerPool: MutableMap<Double, MediaPlayer>
+    get() = Sound.playerPool
   var category: String? = null
   private var mixWithOthers: Boolean = true
   private var focusedPlayerKey: Double? = null
@@ -262,6 +263,7 @@ open class Sound internal constructor(context:ReactApplicationContext):AudioMana
 
 
   fun stop(key: Double, callback: Callback) {
+    cancelStopAfter(key)
     val player = playerPool[key]
     if (player != null && player.isPlaying) {
       player.pause()
@@ -285,6 +287,7 @@ open class Sound internal constructor(context:ReactApplicationContext):AudioMana
 
 
   fun release(key: Double) {
+    cancelStopAfter(key)
     val player = playerPool[key]
     if (player != null) {
       player.reset()
@@ -414,4 +417,38 @@ open class Sound internal constructor(context:ReactApplicationContext):AudioMana
       }
     }
   }
+
+  fun stopAfter(key: Double, seconds: Double) {
+    cancelStopAfter(key)
+    val runnable = Runnable {
+      val player = playerPool[key]
+      if (player != null) {
+        try {
+          if (player.isPlaying) {
+            player.pause()
+            player.seekTo(0)
+          }
+          setOnPlay(false, key)
+        } catch (e: Exception) {
+          Log.e("RNSound", "Error auto-stopping player", e)
+        }
+      }
+      Sound.stopHandlers.remove(key)
+    }
+    Sound.stopHandlers[key] = runnable
+    Sound.mainHandler.postDelayed(runnable, (seconds * 1000).toLong())
+  }
+
+  fun cancelStopAfter(key: Double) {
+    Sound.stopHandlers[key]?.let {
+      Sound.mainHandler.removeCallbacks(it)
+      Sound.stopHandlers.remove(key)
+    }
+  }
+
+  companion object {
+    val playerPool: MutableMap<Double, MediaPlayer> = java.util.concurrent.ConcurrentHashMap()
+    val stopHandlers: MutableMap<Double, Runnable> = java.util.concurrent.ConcurrentHashMap()
+    val mainHandler = android.os.Handler(android.os.Looper.getMainLooper())
+  }
 }
diff --git a/node_modules/react-native-sound/android/src/main/java/com/zmxv/RNSound/SoundModule.kt b/node_modules/react-native-sound/android/src/main/java/com/zmxv/RNSound/SoundModule.kt
index af25df1..0f33be1 100644
--- a/node_modules/react-native-sound/android/src/main/java/com/zmxv/RNSound/SoundModule.kt
+++ b/node_modules/react-native-sound/android/src/main/java/com/zmxv/RNSound/SoundModule.kt
@@ -88,6 +88,16 @@ class SoundModule internal constructor(context: ReactApplicationContext) :
     // Keep: Needed for RN built in Event Emitter Calls
   }
 
+  @ReactMethod
+  override fun stopAfter(key: Double, seconds: Double) {
+    module.stopAfter(key, seconds)
+  }
+
+  @ReactMethod
+  override fun cancelStopAfter(key: Double) {
+    module.cancelStopAfter(key)
+  }
+
   override fun getName(): String {
     return NAME
   }
diff --git a/node_modules/react-native-sound/android/src/newarch/SoundSpec.kt b/node_modules/react-native-sound/android/src/newarch/SoundSpec.kt
index 0adc511..7782305 100644
--- a/node_modules/react-native-sound/android/src/newarch/SoundSpec.kt
+++ b/node_modules/react-native-sound/android/src/newarch/SoundSpec.kt
@@ -39,6 +39,14 @@ open class SoundSpec internal constructor(context: ReactApplicationContext) :
     module.release(key)
   }
 
+  override fun stopAfter(key: Double, seconds: Double) {
+    module.stopAfter(key, seconds)
+  }
+
+  override fun cancelStopAfter(key: Double) {
+    module.cancelStopAfter(key)
+  }
+
   override fun setVolume(key: Double, left: Double, right: Double) {
     module.setVolume(key,left.toFloat(),right.toFloat())
   }
diff --git a/node_modules/react-native-sound/android/src/oldarch/SoundSpec.kt b/node_modules/react-native-sound/android/src/oldarch/SoundSpec.kt
index 5324012..16bd239 100644
--- a/node_modules/react-native-sound/android/src/oldarch/SoundSpec.kt
+++ b/node_modules/react-native-sound/android/src/oldarch/SoundSpec.kt
@@ -14,6 +14,8 @@ abstract class SoundSpec internal constructor(context: ReactApplicationContext)
   abstract fun stop(key: Double, callback: Callback)
   abstract fun reset(key: Double)
   abstract fun release(key: Double)
+  abstract fun stopAfter(key: Double, seconds: Double)
+  abstract fun cancelStopAfter(key: Double)
   abstract fun setVolume(key: Double, left: Double, right: Double)
   abstract fun getSystemVolume(callback: Callback)
   abstract fun setSystemVolume(value: Double)
diff --git a/node_modules/react-native-sound/ios/RNSound.h b/node_modules/react-native-sound/ios/RNSound.h
index f3e14d5..9cf471d 100644
--- a/node_modules/react-native-sound/ios/RNSound.h
+++ b/node_modules/react-native-sound/ios/RNSound.h
@@ -1,12 +1,26 @@
 #import <AVFoundation/AVFoundation.h>
+
+#if __has_include(<React/RCTEventEmitter.h>)
 #import <React/RCTEventEmitter.h>
+#elif __has_include("RCTEventEmitter.h")
+#import "RCTEventEmitter.h"
+#else
+#import <React-Core/RCTEventEmitter.h>
+#endif
 
 #ifdef RCT_NEW_ARCH_ENABLED
   #import <RNSoundSpec/RNSoundSpec.h>
 
 @interface RNSound : RCTEventEmitter  <NativeSoundIOSSpec,AVAudioPlayerDelegate>
 #else
+#if __has_include(<React/RCTBridgeModule.h>)
 #import <React/RCTBridgeModule.h>
+#elif __has_include("RCTBridgeModule.h")
+#import "RCTBridgeModule.h"
+#else
+#import <React-Core/RCTBridgeModule.h>
+#endif
+
 @interface RNSound : RCTEventEmitter <RCTBridgeModule, AVAudioPlayerDelegate>
 #endif
 
diff --git a/node_modules/react-native-sound/ios/RNSound.mm b/node_modules/react-native-sound/ios/RNSound.mm
index 94b2335..b0d6051 100644
--- a/node_modules/react-native-sound/ios/RNSound.mm
+++ b/node_modules/react-native-sound/ios/RNSound.mm
@@ -9,6 +9,7 @@
 @implementation RNSound {
     NSMutableDictionary *_playerPool;
     NSMutableDictionary *_callbackPool;
+    NSMutableDictionary *_stopTimers;
     double _key;  // Add this line to declare _key
 }
 
@@ -62,6 +63,13 @@ - (NSMutableDictionary *)callbackPool {
     return _callbackPool;
 }
 
+- (NSMutableDictionary *)stopTimers {
+    if (!_stopTimers) {
+        _stopTimers = [NSMutableDictionary new];
+    }
+    return _stopTimers;
+}
+
 - (AVAudioPlayer *)playerForKey:(double)key {
     NSNumber *keyNumber = @(key);
     return [[self playerPool] objectForKey:keyNumber];
@@ -250,6 +258,7 @@ - (NSString *)categoryForName:(NSString *)categoryName {
 }
 
 RCT_EXPORT_METHOD(stop:(double)key callback:(RCTResponseSenderBlock)callback) {
+    [self cancelStopAfter:key];
     AVAudioPlayer *player = [self playerForKey:key];
     if (player) {
         [player stop];
@@ -260,6 +269,7 @@ - (NSString *)categoryForName:(NSString *)categoryName {
 
 RCT_EXPORT_METHOD(release:(double)key) {
     @synchronized(self) {
+        [self cancelStopAfter:key];
         AVAudioPlayer *player = [self playerForKey:key];
         if (player) {
             NSNumber *myNumber = @(key);
@@ -304,6 +314,42 @@ - (NSString *)categoryForName:(NSString *)categoryName {
     }
 }
 
+RCT_EXPORT_METHOD(stopAfter:(double)key seconds:(double)seconds) {
+    [self cancelStopAfter:key];
+    
+    NSNumber *keyNumber = @(key);
+    
+    dispatch_async(dispatch_get_main_queue(), ^{
+        @synchronized(self) {
+            NSTimer *timer = [NSTimer scheduledTimerWithTimeInterval:seconds
+                                                             repeats:NO
+                                                               block:^(NSTimer * _Nonnull t) {
+                @synchronized(self) {
+                    AVAudioPlayer *player = [self playerForKey:key];
+                    if (player) {
+                        [player stop];
+                        player.currentTime = 0;
+                        [self setOnPlay:NO forPlayerKey:key];
+                    }
+                    [[self stopTimers] removeObjectForKey:keyNumber];
+                }
+            }];
+            [[self stopTimers] setObject:timer forKey:keyNumber];
+        }
+    });
+}
+
+RCT_EXPORT_METHOD(cancelStopAfter:(double)key) {
+    @synchronized(self) {
+        NSNumber *keyNumber = @(key);
+        NSTimer *timer = [[self stopTimers] objectForKey:keyNumber];
+        if (timer) {
+            [timer invalidate];
+            [[self stopTimers] removeObjectForKey:keyNumber];
+        }
+    }
+}
+
 #pragma mark - Playback Controls
 
 
diff --git a/node_modules/react-native-sound/src/NativeSoundAndroid.ts b/node_modules/react-native-sound/src/NativeSoundAndroid.ts
index ed2b8b5..e060df6 100644
--- a/node_modules/react-native-sound/src/NativeSoundAndroid.ts
+++ b/node_modules/react-native-sound/src/NativeSoundAndroid.ts
@@ -5,6 +5,8 @@ interface SoundOptionTypes {
   loadSync: boolean;
 }
 export interface Spec extends TurboModule {
+  stopAfter: (key: number, seconds: number) => void;
+  cancelStopAfter: (key: number) => void;
   setVolume: (key: number, left: number, right: number) => void;
   setSpeed: (key: number, speed: number) => void;
   setSystemVolume: (value: number) => void;
diff --git a/node_modules/react-native-sound/src/NativeSoundIOS.ts b/node_modules/react-native-sound/src/NativeSoundIOS.ts
index a55307c..642ba02 100644
--- a/node_modules/react-native-sound/src/NativeSoundIOS.ts
+++ b/node_modules/react-native-sound/src/NativeSoundIOS.ts
@@ -11,6 +11,8 @@ export interface Spec extends TurboModule {
     NSLibraryDirectory: string;
     NSCachesDirectory: string;
   };
+  stopAfter: (key: number, seconds: number) => void;
+  cancelStopAfter: (key: number) => void;
   setVolume: (key: number, left: number, right: number) => void;
   setMode: (value: string) => void;
   setSpeed: (key: number, speed: number) => void;
